/* D-03 3단계 검산 · /ledger/verify/:predictionId
   담당 스토리 [ANT-FE-VERIFY]
   설계서 docs/화면설계서.md §3 · §4 D-03 의 제약을 보고 이 자리를 채운다. */
export default function Verify() {
  return (
    <main className="main">
      <div className="main-inner">
        <div className="page-head">
          <h1>3단계 검산</h1>
          <p>{'D-03 · /ledger/verify/:predictionId'}</p>
        </div>
        <div className="placeholder tall">{'[ANT-FE-VERIFY] 에서 구현합니다'}</div>
      </div>
    </main>
  )
}
