.env
.claude/

# SSH·TLS 개인키 (EC2 접속용 pem 등). 저장소에 들어가면 서버가 통째로 열린다.
*.pem
*.ppk
*.key

# 개인별 로컬 오버라이드. .env.example·.env.production 은 공개값이라 추적한다.
.env.local
.env.*.local

# macOS
.DS_Store
.AppleDouble
.LSOverride
._*
.Spotlight-V100
.Trashes

# Windows
Thumbs.db
ehthumbs.db
Desktop.ini
$RECYCLE.BIN/
*.lnk

# local tooling
.erd-render/

# 대외비 문서 (커밋 금지)
docs/토큰증권_API_명세.md
docs/금융망_API_명세.md
